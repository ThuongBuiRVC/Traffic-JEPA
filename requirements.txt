# Traffic-JEPA — pinned to the exact environment that produced the top-1 result.
# torch/vision are CUDA 13.0 wheels; install with the cu130 index (see Dockerfile).
torch==2.12.0
torchvision==0.27.0
transformers==5.8.1
tokenizers==0.22.2
safetensors==0.7.0
huggingface-hub==1.15.0
accelerate==1.13.0
timm==1.0.27
einops==0.8.2
decord==0.6.0
opencv-python-headless==4.13.0.92
numpy==2.4.5
rich==15.0.0
