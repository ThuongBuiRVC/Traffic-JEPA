# Traffic-JEPA — pinned to the environment that produced the top-1 result.
# torch/vision are CUDA 13.0 wheels, install with the cu130 index (see the README).
torch==2.12.0
numpy==2.4.5

# pulled in by torch.hub for V-JEPA 2.1: hubconf declares timm + einops, and its
# preprocessor imports torchvision.transforms — all three are loaded on `torch.hub.load`.
torchvision==0.27.0
timm==1.0.27
einops==0.8.2

# Llama-3.2-1B predictor, EmbeddingGemma option vectors, Qwen3-VL captioner
transformers==5.8.1
tokenizers==0.22.2
safetensors==0.7.0
huggingface-hub==1.15.0
click==8.4.2  # huggingface-hub's `hf` CLI needs this but doesn't pull it in itself
accelerate==1.13.0
peft==0.19.1

# video/frame IO + progress
opencv-python-headless==4.13.0.92
pillow==11.3.0
tqdm==4.67.1
rich==15.0.0

# only to retrain the caption LoRA (traffic_jepa.captioning.train_lora)
datasets==4.4.1
sentencepiece==0.2.1
